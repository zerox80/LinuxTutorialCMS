# Database Configuration
# Path to SQLite database file
# In Docker: sqlite:/data/database.db
# In Development: sqlite:./database.db
DATABASE_URL=sqlite:./database.db

# JWT Configuration
# Secret key for JWT token signing and verification
# CRITICAL: Must be changed to a strong, random secret in production!
# Generate with: openssl rand -base64 32
JWT_SECRET=CHANGE-THIS-TO-A-SECURE-RANDOM-SECRET-IN-PRODUCTION

# Server Configuration
# Port on which the backend server will run
PORT=8489
FRONTEND_ORIGINS=http://localhost:5173,http://localhost:3000

# Admin Credentials (used to bootstrap default admin user)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# Logging Configuration
# Rust log level (trace, debug, info, warn, error)
RUST_LOG=info
